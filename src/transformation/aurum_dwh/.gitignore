# dbt build artifacts — regenerated by every run, never committed.
# package-lock.yml is deliberately NOT ignored: it pins resolved package
# versions, matching how uv.lock is committed and drift-checked in CI.
target/
dbt_packages/
logs/
*.log
.user.yml
.env
